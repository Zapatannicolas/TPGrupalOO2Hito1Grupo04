<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN" "https://hibernate.org/dtd/hibernate-mapping-3.0.dtd">

<hibernate-mapping>

	<class name="datos.Festival" table="festival">

		<!-- ID -->
		<id name="id" column="idFestival">
			<generator class="identity" />
		</id>

		<!-- Atributos - Columnas -->
		<property name="nombre" column="nombre" type="string" not-null="true" />

		<property name="temporada" column="temporada" type="string"
			not-null="true" />

		<property name="fechaInicio" column="fechaInicio" type="LocalDate"
			not-null="true" />

		<property name="fechaFin" column="fechaFin" type="LocalDate"
			not-null="true" />

		<!-- Relacion con listas (otras clases) -->
		<set name="unidadesVenta" table="unidadventas">
			<key column="idFestival" />
			<one-to-many class="datos.UnidadVenta" />
		</set>
	</class>


</hibernate-mapping>